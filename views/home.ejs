<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ANON MEMBERS</title>
  </head>
  <body>
    <header>anon clubhouse feed</header>
    <% if (user) { %>
    <p>Welcome, <%= user.username %></p>
    <a href="/create-post" class="...">+ New Post</a>
    <a href="/logout">Log Out</a>
    <% } else { %>
    <div class="guest-banner ...">
      Browsing as a guest. <a href="/register">Register</a> or
      <a href="/login">Log in</a> to join the conversation.
    </div>
    <% } %>
    <% if (posts && posts.length > 0) { %> <% posts.forEach(post => { %>
    <div class="post">
      <span><%= getDisplayName(post, user) %></span>

      <p><%= post.content %></p>

      <% if (user && (user.id === post.user_id || user.is_admin)) { %>
      <!-- options menu: edit/delete -->
      <% } %>
    </div>
    <% }) %>
    <% } %>
  </body>
</html>
